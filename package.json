{
  "scripts": {
    "build": "docker build -t idle-miner-bot .",
    "start": "docker run --restart always idle-miner-bot"
  },
  "type": "module",
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "fs": "^0.0.1-security",
    "storelite2": "^0.1.3"
  }
}
